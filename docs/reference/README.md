# Reference

This folder stores canonical architecture references.

## Primary Reference

The primary reference PDF should be stored here:

`ai_market_machine_ingestion_architecture_framework_v2.pdf`

## Relationship To `docs/`

- Markdown files in `docs/` are the active operational rules.
- The PDF in this folder is the source architecture reference.
- The markdown docs should stay aligned with the source reference, but they are the working rules for the repository.

## Future References

Future architecture PDFs should be added here as reference material, not mixed into application code or `app/` modules.
