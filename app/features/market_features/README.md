# app/features/market_features

This package hosts the unified dry-run market feature bundle layer.

The individual feature domains remain in their own packages; this package only aggregates their deterministic dry-run outputs.

