# app/features/market_regime

This package hosts a deterministic dry-run market regime synthesizer.

It combines existing bundle summary evidence into a higher-level regime view without vendor calls, database writes, scheduler activation, or AI decision logic.

