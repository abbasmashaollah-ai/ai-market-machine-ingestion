# app/features/event_calendar

This package hosts the deterministic dry-run event calendar feature slice.

It assembles fixture-only event evidence and reports without vendor calls, database writes, or scheduler activation.
