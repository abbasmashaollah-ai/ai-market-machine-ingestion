# app/features/earnings

This package hosts a deterministic dry-run earnings evidence slice.

It builds fixture-only earnings observations and reports without vendor calls, database writes, scheduler activation, or AI decision logic.

