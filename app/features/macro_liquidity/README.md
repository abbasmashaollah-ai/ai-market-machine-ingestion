# app/features/macro_liquidity

This package hosts a deterministic dry-run macro liquidity synthesizer.

It combines existing bundle evidence into a higher-level liquidity view without vendor calls, database writes, scheduler activation, or AI decision logic.

