# app/features/market_risk

This package hosts a deterministic dry-run market risk synthesizer.

It combines existing bundle and synthesized evidence into a higher-level market risk view without vendor calls, database writes, scheduler activation, or AI decision logic.

