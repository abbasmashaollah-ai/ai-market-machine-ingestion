# liquidity_rates

Deterministic liquidity and rates evidence helpers for dry-run use only.

This package builds fixture-driven observations, validates them, writes them to an in-memory mock writer, and produces JSON-friendly dry-run reports. Real writer and persistence behavior remains deferred.