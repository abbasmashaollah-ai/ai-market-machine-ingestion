# cross_asset

Deterministic cross-asset and intermarket evidence helpers for dry-run use only.

This package builds observations from fixture close histories, validates them, writes them to an in-memory mock writer, and produces JSON-friendly dry-run reports. Real writer and persistence behavior remains deferred.